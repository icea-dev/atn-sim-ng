#
# CORE
# Copyright (c)2010-2012 the Boeing Company.
# See the LICENSE file included in this distribution.
#
''' Sample user-defined service.
'''

import os

from core.service import CoreService, addservice
from core.misc.ipaddr import IPv4Prefix, IPv6Prefix

class AIMod(CoreService):
    ''' This is a sample user-defined service. 
    '''
    # a unique name is required, without spaces
    _name = "AIMod"
    # you can create your own group here
    _group = "Aviation"
    # list of other services this service depends on
    _depends = ()
    # per-node directories
    _dirs = ()
    # generated files (without a full path this file goes in the node's dir,
    #  e.g. /tmp/pycore.12345/n1.conf/)
    _configs = ('aimod.sh', 'aimod.cfg')
    # this controls the starting order vs other enabled services
    _startindex = 50
    # list of startup commands, also may be generated during startup
    _startup = ('sh aimod.sh',)
    # list of shutdown commands
    _shutdown = ()

    @classmethod
    def generateconfig(cls, node, filename, services):
        ''' Return a string that will be written to filename, or sent to the
            GUI for user customization.
        '''
        cfg = ""

        if filename == "aimod.sh":
            cfg += "#!/bin/sh\n"
            cfg += "# auto-generated by AIMod (aimod.py)\n"
            cfg += "python -m adsb.aimod.aimod\n"

        elif filename == "aimod.cfg":
            cfg += "[General]\n"
            cfg += "id = MLAT-SRV\n"
            cfg += "\n"
            cfg += "; DB where unprocessed messsages are stored\n"
            cfg += "dbname = mlat\n"
            cfg += "dbuser = mlat\n"
            cfg += "dbpass = mlat\n"
            cfg += "dbhost = 172.17.255.254\n"
            cfg += "\n"
            cfg += "; Destination(s) of reliable messages\n"
            cfg += "destinations = Dump1090\n"
            cfg += "\n"
            cfg += "[Dump1090]\n"
            cfg += "type = dump1090\n"
            cfg += "server = 127.0.0.1\n"
            cfg += "port = 30001\n"
        else:
            cfg += "# %s not defined\n" % filename

        return cfg

    @staticmethod
    def subnetentry(x):
        ''' Generate a subnet declaration block given an IPv4 prefix string
            for inclusion in the config file.
        '''
        if x.find(":") >= 0:
            # this is an IPv6 address
            return ""
        else:
            net = IPv4Prefix(x)
            return 'echo "  network %s"' % (net)

# this line is required to add the above class to the list of available services
addservice(AIMod)

